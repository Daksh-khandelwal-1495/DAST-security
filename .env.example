# Slack Configuration
# Copy this file to .env and fill in your actual values

# Option 1: Use Slack Bot Token (Recommended)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_CHANNEL=#security-alerts

# Option 2: Use Slack Webhook URL (Alternative)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# ============================================
# AI Classification Configuration (Quality Optimized)
# ============================================
# Priority Order: Gemini (Quality) → Groq (Speed) → OpenAI (Alternative) → Pattern Matching (Fallback)

# PRIMARY: Google Gemini API (Recommended for QUALITY)
# Free Tier: 15 requests/min, 1,500 requests/day
# Get your FREE key: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# SECONDARY: Groq API (Optional - for SPEED)
# Free Tier: 30 requests/min, 14,400 requests/day
# Get your FREE key: https://console.groq.com/keys
# GROQ_API_KEY=your-groq-api-key-here

# TERTIARY: OpenAI API (Optional - ALTERNATIVE, requires payment)
# Only needed if you want to use GPT-4 instead
# Get key: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your-openai-api-key-here

# Enable/disable AI classification (set to false to use pattern matching only)
USE_AI_CLASSIFICATION=true

# ZAP Report Configuration
ZAP_REPORT_PATH=report.json

# ============================================
# Build Breaker Configuration
# ============================================
# Control whether the build should fail based on vulnerability counts

# Maximum allowed vulnerabilities by severity (fail build if exceeded)
MAX_CRITICAL_VULNS=0        # Zero tolerance for critical issues
MAX_HIGH_VULNS=3            # Allow up to 3 high severity issues
MAX_MEDIUM_VULNS=10         # Allow up to 10 medium severity issues

# AI severity threshold (0-10 scale) - flag vulnerabilities above this score
AI_SEVERITY_THRESHOLD=8.0

# Enable/disable build breaking (set to false for report-only mode)
BREAK_BUILD_ON_CRITICAL=true

# GitHub Integration (automatically set by GitHub Actions)
# GITHUB_PR_NUMBER=